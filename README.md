# expert-succotash
Four-part NLP coursework spanning classical pipelines to transformer explainability — sentiment/intent/topic modeling, phishing detection (LR→FNN→RNN→LSTM/BiLSTM+Attention), embedding evolution (TF-IDF→Word2Vec→BERT), and fine-tuned BERT with SHAP/LIME interpretability.
